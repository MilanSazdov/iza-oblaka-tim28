# iza-oblaka-tim28

<!--
What goes here:
- Project overview (data lake on AWS: Bronze / Silver / Gold)
- Architecture diagram or short description (VPC, IAM, S3, Lambdas, EventBridge)
- Repo layout (.github/workflows, infra/, lambdas/)
- How to run locally / deploy:
    - Prereqs (Terraform >= 1.x, AWS CLI, Python 3.x)
    - `cd infra && terraform init && terraform plan && terraform apply`
- Required GitHub Secrets for the deploy workflow (AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, etc.)
- Team members / credits
-->
